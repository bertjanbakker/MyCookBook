This example demonstrates the advantages of custom loggers.

It shows
- not using the root logger and not calling basicConfig()
- a separate logger for each module
- separate log files, levels and formats for each logger